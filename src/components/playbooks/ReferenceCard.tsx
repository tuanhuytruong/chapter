import { Link } from "react-router-dom";
import { ChefHat, Sigma, ListChecks } from "lucide-react";
import type { ReferenceCard } from "../../types";
const icon={recipe:ChefHat,formula:Sigma,procedure:ListChecks}; const label={recipe:"Recipe",formula:"Formula",procedure:"Procedure"};
export default function ReferenceCard({card}:{card:ReferenceCard}) { const Icon=icon[card.cardType]; return <Link to={`/playbooks/${card.id}`} className="block rounded-2xl border border-natural-border bg-natural-cream p-4 no-underline transition hover:border-natural-sage focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-natural-sage"><p className="flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-wider text-natural-sage"><Icon className="h-3.5 w-3.5"/>{label[card.cardType]}</p><h2 className="mt-1 text-base font-bold text-natural-dark">{card.title}</h2><p className="mt-1 line-clamp-2 text-sm text-natural-stone">{card.content}</p>{card.tags.length>0&&<div className="mt-3 flex flex-wrap gap-1">{card.tags.map(tag=><span key={tag} className="rounded-full bg-natural-sage/10 px-2 py-1 text-[10px] font-bold text-natural-sage">{tag}</span>)}</div>}<p className="mt-3 text-[11px] text-natural-stone">{card.book_title} · {card.file_type === "pdf" ? "Pages" : "Chunks"} {card.sourcePageStart}–{card.sourcePageEnd} · {card.last_used_at ? "Used" : "Not used yet"}</p></Link>; }
