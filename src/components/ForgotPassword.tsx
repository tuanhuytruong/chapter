import { FormEvent, useState } from "react";
import { Link } from "react-router-dom";

export default function ForgotPassword() {
  const [email, setEmail] = useState(""); const [done, setDone] = useState(false); const [busy, setBusy] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { await fetch("/api/auth/forgot-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); } finally { setBusy(false); setDone(true); } }
  return <main className="min-h-screen bg-natural-bg p-4 font-sans"><section className="mx-auto mt-[12vh] w-full max-w-sm rounded-[28px] border border-natural-border bg-natural-cream p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-widest text-natural-sage">Chapter</p><h1 className="mt-2 font-sans text-2xl font-bold text-natural-dark">Reset your password</h1>{done ? <div aria-live="polite" className="mt-4 space-y-4 text-sm leading-relaxed text-natural-stone"><p>If an account matches this email, we’ve sent a reset link.</p><Link className="font-bold text-natural-sage underline" to="/login">Back to sign in</Link></div> : <form onSubmit={submit} className="mt-5 space-y-4"><p className="text-sm leading-relaxed text-natural-stone">Enter the email connected to your account. We’ll send a one-time reset link.</p><label className="block text-xs font-bold">Email<input required type="email" autoFocus value={email} onChange={(e) => setEmail(e.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-natural-border bg-natural-bg px-3 text-sm outline-none focus:border-natural-sage focus:ring-2 focus:ring-natural-sage/20" /></label><button disabled={busy} className="min-h-11 w-full rounded-full bg-natural-sage text-xs font-bold uppercase tracking-wider text-white disabled:opacity-60">{busy ? "Sending…" : "Send reset link"}</button><Link className="block text-center text-xs font-bold text-natural-stone underline" to="/login">Back to sign in</Link></form>}</section></main>;
}
