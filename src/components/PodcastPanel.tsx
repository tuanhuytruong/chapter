// Podcast selection moved to the dedicated /podcasts route. Kept as a small
// compatibility module until clients with an old cached bundle are refreshed.
export default function PodcastPanel() { return null; }
